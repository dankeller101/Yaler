var React = require('react');

var PrivacyandTos = React.createClass({
  render: function() {
    return <p>
      Yaler will never sell or use any of your personal data outside of the use of the data within the app.
      Additionally, Yaler will only ever use data that is publically available.

      TOS:
      Yaler is not meant to be a registered corporation, company or product.  Yaler is built in satire.
      Do not use Yaler for anything other than comedy.
    </p>;
  },
});

module.exports = PrivacyandTos;
